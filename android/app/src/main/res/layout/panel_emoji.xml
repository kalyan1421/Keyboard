<?xml version="1.0" encoding="utf-8"?>
<!-- CleverType Style Emoji Panel -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/emojiPanelRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="0dp"
    android:background="@color/kb_panel_bg"
    android:clipToPadding="false">

    <!-- Category Icons Bar (Horizontal Scrolling) -->
    <HorizontalScrollView
        android:id="@+id/emojiCategoryScroll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:overScrollMode="never"
        android:fadeScrollbars="true"
        android:background="@color/kb_panel_bg">

        <LinearLayout
            android:id="@+id/emojiCategories"
            android:orientation="horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="8dp"
            android:paddingVertical="8dp"
            android:gravity="center_vertical">
            <!-- Categories will be dynamically added here:
                 🔍 Search | ⏰ Recent | 😊 Smileys | 🏃 Activities | 📋 Objects | 😃 Emoji | 💡 Symbols | 🚩 Flags -->
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Emoji Grid (Scrollable Area) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/emojiGrid"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:overScrollMode="never"
        android:paddingHorizontal="8dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false"
        android:background="@color/kb_panel_bg"/>

    <!-- Bottom Bar with ABC Button -->
    <LinearLayout
        android:id="@+id/emojiBottomBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="start|center_vertical"
        android:paddingHorizontal="8dp"
        android:paddingVertical="8dp"
        android:background="@color/kb_panel_bg">

        <!-- ABC Button (Return to Keyboard) -->
        <TextView
            android:id="@+id/btnEmojiToABC"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:paddingHorizontal="20dp"
            android:paddingBottom="12dp"
            android:gravity="center"
            android:text="ABC"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/kb_text_primary"
            android:background="@android:color/transparent"
            android:clickable="true"
            android:focusable="true" />

        <!-- Hidden Search Input (for search functionality) -->
        <EditText
            android:id="@+id/emojiSearchInput"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone" />

        <!-- Hidden Delete Button (functionality available in category bar) -->
        <ImageView
            android:id="@+id/btnEmojiDelete"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            android:contentDescription="Delete" />

        <!-- Hidden Search Button -->
        <ImageView
            android:id="@+id/emojiSearchBtn"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="gone"
            android:contentDescription="Search" />
    </LinearLayout>

    <!-- Hidden Output (for compatibility) -->
    <TextView
        android:id="@+id/emojiOutput"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

   

</LinearLayout>
