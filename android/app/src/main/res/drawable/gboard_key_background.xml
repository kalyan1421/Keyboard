<?xml version="1.0" encoding="utf-8"?>
<!-- Gboard-style key background with Material Design 3 styling -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <layer-list>
            <!-- Shadow layer -->
            <item android:top="1dp" android:left="1dp" android:right="1dp" android:bottom="2dp">
                <shape android:shape="rectangle">
                    <solid android:color="#10000000"/>
                    <corners android:radius="@dimen/key_corner_radius"/>
                </shape>
            </item>
            <!-- Main key background -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/gboard_key_pressed"/>
                    <corners android:radius="@dimen/key_corner_radius"/>
                    <stroke android:width="0.5dp" android:color="@color/gboard_key_border"/>
                </shape>
            </item>
        </layer-list>
    </item>
    
    <!-- Normal state -->
    <item>
        <layer-list>
            <!-- Shadow layer -->
            <item android:top="1dp" android:left="1dp" android:right="1dp" android:bottom="2dp">
                <shape android:shape="rectangle">
                    <solid android:color="#08000000"/>
                    <corners android:radius="@dimen/key_corner_radius"/>
                </shape>
            </item>
            <!-- Main key background -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/gboard_key_background"/>
                    <corners android:radius="@dimen/key_corner_radius"/>
                    <stroke android:width="0.5dp" android:color="@color/gboard_key_border"/>
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
